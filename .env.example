PORT=3000
# Comma-separated browser origins allowed to call /mcp. Non-browser MCP clients
# normally do not send Origin. Leave empty unless a browser client needs access.
ALLOWED_ORIGINS=http://localhost:5173
